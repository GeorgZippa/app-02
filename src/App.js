import React, {useState} from 'react';

const form = () => {
    return(
    <div>
      <input />
    </div>
  );
};

const form = () => {
  const [inputValue, setInputValue] = useState (initialState: '9900')
  return(
    <div>
      <input value={inputValue} />
    </div>
  );
};